关于rpc和http

所以我个人是把rpc用于对公司内部的项目。把http作为对外项目。

这两个根本不是一个类型的东西啊，RPC可以通过HTTP来实现，也可以直接通过Socket TCP实现，HTTP本质是一种网络通信协议，而RPC则是一种网络编程模式或者说是分布式的编程模式。RPC的好处你可以这样理解，HTTP适合对外通信，RPC适合对内。比如一般把向外发布的Web服务都定义为基于HTTP的Rest API，而一个比较大型的互联网应用都是由很多子服务在内部构建起来的，这些子服务彼此之间也需要沟通信息，此时，RPC就可以集成这些子服务，好处是这些子服务之间没有耦合，可以热更新，一个子服务出问题不会影响整体，子服务就像是网络环境下的进程，可以是不同编程语言写的，像库一样，可以互相调用，彼此只要遵守一种通信格式就可以了。RPC因为是内部服务，所以很多时候服务在一个网里，此时就没必要用HTTP短链接了，因为HTTP是无状态的，可以直接用长连接，这样节省资源，也可以基于HTTP实现，这样也有些好处，比如可以没必要处理各种断线之类的问题。

所以RPC框架少有用HTTP的，多是使用socket或者消息队列。

只要是远程调用都可以叫RPC，和是不是通过http没什么关系。

rpc框架的底层可以是http. GRPC的底层就是HTTP2

